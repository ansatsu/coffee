# Local development against the docker compose db (host port 5433).
# Inside docker compose, DATABASE_URL is provided by the environment instead.
DATABASE_URL=postgres://coffee:coffee@localhost:5433/coffee
